# App Environment
NEXT_PUBLIC_APP_ENV=development

# Canonical site URL — used to resolve OG/Twitter image URLs in metadata.
# Set to your production domain (e.g. https://foodly.kr) before deploy.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
